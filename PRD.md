# 软件集成工作台第一版 PRD

## 1. 背景

GRDP-Studio 由团队共同开发，当前解析融合和单井产能功能持续演进。软件集成模块由独立功能分支开发，第一版先建立符合浏览器操作习惯的正式入口和工作区，不连接 PIPESIM、ECLIPSE、CMG 等本地软件。

Avalonia 项目 `/Users/xishun/code/Bei/Avalonia_oil` 仅作为视觉、信息结构和交互状态参考。Web 端沿用 GRDP-Studio 现有 Vue、Element Plus 和 SCSS 实现，不照搬桌面 Docking、MDI、多窗口、双击文档或固定窗口尺寸等机制。

## 2. 目标

- 在现有 `/ipr` 工作台中建立与“解析融合”并列的“软件集成”模式。
- 保留现有 Ribbon、项目树和工作区视觉语言。
- 通过 URL 表达当前工作台模式，支持刷新及浏览器前进、后退。
- 为后续真实软件接入建立单一、稳定的 Web 入口。
- 第一版不制造软件已经接入的假象。

## 3. 用户流程

1. 用户在顶部 Ribbon 点击“软件集成”。
2. 页面进入 `/ipr?workspace=software-integration`，软件集成页签显示黄色活动状态。
3. Ribbon 按 Avalonia 的软件控制台信息架构显示项目管理、数值模拟、井筒模拟、管网模拟、一体化模拟、模拟器设置和可视化七个分组。
4. 中央区域显示“软件集成”概览空状态，引导用户从上方选择功能。
5. 用户点击任一功能入口时，页面顶部显示“该功能正在开发中”的轻量消息，中央概览保持不变。
6. 用户可继续搜索、展开、折叠和选择左侧树节点，但不会打开解析融合业务页面。
7. 用户切回“解析融合”后，恢复进入软件集成前的解析融合内容和节点选择状态。
8. 用户在单井产能工作台点击“软件集成”时，统一进入 `/ipr?workspace=software-integration`。

## 4. 第一版范围

### 4.1 完成内容

- Ribbon 页签切换事件和受控活动页签。
- `/ipr` 软件集成模式及 URL 状态同步。
- 软件集成中央概览空状态。
- Ribbon 各命令入口的非阻塞“正在开发中”提示。
- 使用 GRDP-Studio 现有 SVG 资源为 Ribbon 命令配置语义不同、用途合适的图标。
- 软件集成模式下的项目树非业务选择行为。
- 解析融合原页面和节点状态恢复。
- 单井产能页面到统一软件集成工作区的导航。
- 常见笔记本和桌面浏览器宽度适配；窄窗口不明显撑破布局。

### 4.2 暂不完成

- 不连接真实 PIPESIM、ECLIPSE 或 CMG。
- 不新增 Spring Boot 接口、数据库表或配置实体。
- 不实现模型上传、文件选择、参数填写、运行、取消和结果展示。
- 不为 Ribbon 命令分别创建业务页面。
- 不提供模拟连接状态、模拟计算结果或固定成功结果。
- 不实现桌面 Docking、MDI、多窗口、底部求解器面板和双击打开文档。
- 不要求手机浏览器具备完整工作台操作体验。
- 不修改解析融合和单井产能的计算逻辑。

## 5. 交互规则

- “软件集成”使用现有 Ribbon 黄色活动态。
- 软件集成 Ribbon 分组和命令以 Avalonia 控制台为信息结构参考，但使用现有 Web Ribbon 的横向滚动、点击和响应式行为。
- Ribbon 命令使用稳定按钮宽度和固定图标区域；长名称允许居中换行，不得侵入相邻命令或分组。
- 笔记本宽度不足以完整显示全部分组时保留各命令可读尺寸，通过 Ribbon 横向滚动查看后续分组，不通过压缩文字制造重叠。
- 软件集成 Ribbon 包含：
  - 项目管理：新建项目、导入模型、保存。
  - 数值模拟：ECLIPSE、INTERSECT、扩展接口。
  - 井筒模拟：PIPESIM 井筒、wellcat、扩展接口。
  - 管网模拟：PIPESIM Network、wellcat、扩展接口。
  - 一体化模拟：加载模拟、一体化建模、PVT设置、井信息匹配、耦合参数传递、模拟控制、耦合计算、运行状态监控。
  - 模拟器设置：模拟器设置。
  - 可视化：2D可视化、3D可视化、曲线。
- 软件集成 URL 为 `/ipr?workspace=software-integration`。
- 刷新页面后仍停留在软件集成模式。
- 浏览器前进和后退必须正确恢复 URL 对应的工作台模式。
- 软件集成概览文案为“软件集成”和“请从上方选择需要使用的功能”。
- 未开发提示格式为“`<功能名> 功能正在开发中`”，使用 Element Plus 轻量消息并自动关闭。
- 软件集成模式下，项目树保留搜索、侧栏收起、展开、折叠和选择视觉。
- 软件集成模式下，树节点选择不得打开业务页面、跳转单井产能或触发删除菜单。
- 软件集成中的节点选择不得覆盖解析融合原有的页面和节点选择状态。

## 6. 技术约束

- 使用现有 Vue 3、JavaScript、Vue Router、Element Plus 和 SCSS。
- 不新增或升级 npm 依赖。
- 复用 `RibbonMenu.vue` 和 `WorkspaceSidebar.vue`。
- 软件集成只在 `/ipr` 中渲染，避免在多个工作台维护重复实现。
- `RibbonMenu.vue` 新增能力必须保持现有调用方默认行为不变。
- 第一版为纯前端改动。

## 7. 验收标准

- 点击“软件集成”后，URL、Ribbon 和中央概览同步切换。
- 刷新、前进和后退后，工作台模式与 URL 一致。
- Ribbon 各命令均显示正确提示，且不替换中央概览。
- 软件集成模式下操作项目树不会打开解析融合内容。
- 切回解析融合后，原业务页面和原节点选择保持不变。
- 从单井产能工作台可进入统一的软件集成工作区。
- 解析融合和单井产能原有命令行为不发生回归。
- 不包含后端、数据库和真实外部软件调用改动。

## 8. 基线同步记录

开发期间主线曾缺少 `vue/src/utils/modifiedIsochronalTree.js` 和 `vue/src/api/productivityTests.js`。后续 `main@9bc5e15` 已补齐这两个文件，当前功能分支已同步该基线，前端全量构建恢复正常。
