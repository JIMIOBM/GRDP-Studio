# L26：PIPESIM Network Choke Bean Size 受控方案验收

## 状态

- 官方 Python Toolkit 隔离副本尖峰：通过。
- Worker/Spring/Vue 代码与自动化：通过。
- 当前 `5173` 浏览器部署：待人工验收；不能用官方脚本结果替代部署证据。
- 解析融合模块：本任务未修改。

## 官方依据与模型

- Toolkit 示例：`C:\Program Files\Schlumberger\PIPESIM2022.1\Developer Tools\Python Toolkit\Examples\get_set_value.py`
- Toolkit 帮助：`C:\Program Files\Schlumberger\PIPESIM2022.1\Developer Tools\Python Toolkit\Help`
- 官方模型：`C:\Program Files\Schlumberger\PIPESIM2022.1\Case Studies\Network Models\CSN_301_Small Network.pips`
- Study：`Study 1`
- Choke：`Choke`
- 官方读取值：`BeanSize = 2.0 in`
- 验收方案值：`BeanSize = 3.0 in`

实现使用官方 `Parameters.Choke.BEANSIZE`，不根据页面字段名猜测单位；只在隔离运行副本调用 `set_value`，写入后调用 `get_value` 回读，回读通过后才执行 Network Simulation。

## 已完成的代码合同

方案参数合同为 `pipesim-network-choke-bean-size-parameters/1`，字段严格为：

```json
{
  "schemaVersion": "pipesim-network-choke-bean-size-parameters/1",
  "baselineRunId": 1001,
  "choke": "Choke",
  "originalBeanSize": 2.0,
  "targetBeanSize": 3.0
}
```

检查合同为 `pipesim-network-inspection/3`，其中包含 `chokes[]` 的名称、真实 Bean Size 和官方单位。后端与 Worker 均拒绝未知 Choke、错 Study/基线、同值、非有限值、非正值、路径穿越和原值回读不一致。

## 官方尖峰证据

在 `C:\GRDP-TestOutput\l26-spike-20260923` 使用官方 Toolkit：

1. 基线副本保持 `Choke BeanSize=2.0 in`，Network Simulation 返回 `Completed / VALID_FULL`。
2. 方案副本通过官方 `set_value` 改为 `3.0 in`，回读为 `2.9999999999999996`，在允许浮点误差内通过。
3. 方案 Network Simulation 返回 `Completed / VALID_FULL`。
4. 基线与方案的系统结果发生真实变化；例如方案的 `BottomHolePressure` 返回 `Gas-Sales=710.5140814508189`、`Well=2408.2811216894765`、`Oil-Facility=131.41164163639635`。
5. 控制台 Worker 直连方案执行返回 `ok / VALID_FULL`，并产生审计消息：`Applied Network Choke BeanSize Choke 2.0->2.9999999999999996 in.`。

## 自动化证据

- Worker 定向测试：`158/158` 通过。
- Python L26 官方适配器测试：`65/65` 通过，官方适配器语法检查通过。
- Vue production build：通过；仅有既有 Sass/Rollup/chunk-size 警告。
- 后端 Java 21 + Maven：L26 相关定向测试 `91/91` 通过，源码编译通过。全量测试另有 65 个既有管线测试因仓库缺少 `sql\pipeline_capacity.sql` 报错；本轮不修改管线/解析融合模块，也不把全量测试标为通过。

## 当前 5173 人工验收步骤

1. 确认部署已加载包含 L26 的 Backend、Worker 和 Vue 构建；旧 Worker 不会显示该方案入口。
2. 上传上述官方 `.pips`，等待检查完成；在 Network 结果/检查区域确认出现 `Study 1` 和 `Choke / 2.0 / in`。
3. 先保持“创建 Choke Bean Size 方案”关闭，选择 `Study 1`，运行一次无参数 Network 基线。必须为 `SUCCEEDED` 且结果合同为 `VALID_FULL`，记下基线 Run ID。
4. 重新进入同一模型版本，勾选“创建 Choke Bean Size 方案”。选择刚才的成功基线、`Choke`，确认原值显示 `2.0 in`，目标值输入 `3.0`。
5. 点击运行。页面应显示方案参数、`VALID_FULL`、Network 真实结果、审计消息和与基线的差异；不能只显示“文件已变化”。
6. 刷新页面、切换历史 Run，再确认基线/方案绑定、Study、Choke、原值/目标值和结果没有串联。
7. 查看运行记录/工程包信息，确认上传源模型 SHA-256 不变，运行副本清理完成，未发布用户本机任意路径。

## 放行标准

- [ ] 检查合同为 `/3`，显示真实 Choke 和单位。
- [ ] 基线与方案均由当前部署真实执行，均为 `VALID_FULL`。
- [ ] 方案确实显示 `2.0 in → 3.0 in`，并保留基线 Run 绑定。
- [ ] 结果中存在官方 Network 业务结果差异和 `Applied Network Choke BeanSize` 审计。
- [ ] 源模型哈希不变，失败/回读错误能明确阻止计算。
- [ ] 刷新和历史恢复后数据一致。

若只看到旧的 Network 边界方案、没有 Choke 选择器或仍提示 `INVALID_ADAPTER_PROTOCOL`，说明当前 5173 没有加载 L26 Worker/后端合同，应先重启并确认 generation，不应判定模型不支持。
